/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["static.remove.bg", "lh3.googleusercontent.com"],
  },
};

module.exports = nextConfig;
